# Dependencies
node_modules/

# Secrets (never commit)
.env
.env.*
!.env.example
.dev.vars

# Local tooling
.wrangler/
.claude/settings.local.json

# D1 のバックアップ(個人情報が入る。保存先は、リポジトリの外にする。うっかり置いたときの、保険)
nolito-d1-*.sql
backups/

# Logs / OS
*.log
.DS_Store
Thumbs.db
